---
title: Variáveis de Ambiente
description: Configuração segura de chaves e credenciais
---

# Variáveis de Ambiente

Este documento lista todas as variáveis de ambiente necessárias para rodar a aplicação, com propósito, tipo e exemplos seguros.

---

## Regra de Ouro

❌ **NUNCA** commitar arquivo `.env` com valores reais  
✅ **SEMPRE** usar `.env.example` com placeholders seguro

```bash
# ✅ Bom: .env.example (sem segredos)
GROQ_API_KEY=sk-proj-YOUR_KEY_HERE
DATABASE_URL=postgresql://postgres:PASSWORD@localhost:5432/guia_hospede

# ❌ Ruim: .env (com segredos reais)
GROQ_API_KEY=sk-proj-oFu5...real...key
DATABASE_URL=postgresql://postgres:abc123real@localhost:5432/guia_hospede
```

**No `.gitignore`**:
```
.env
.env.local
.env.*.local
```

---

## Backend

### Obrigatórias

| Variável | Propósito | Tipo | Exemplo Seguro |
|----------|-----------|------|-----------------|
| `NODE_ENV` | Modo de execução (dev/prod) | String | `development` ou `production` |
| `DATABASE_URL` | Conexão PostgreSQL | String (DSN) | `postgresql://postgres:seazone_dev@localhost:5432/guia_hospede` |
| `GROQ_API_KEY` | Chave API do Groq (IA) | String (Secret) | `sk-proj-YOUR_API_KEY_HERE` |

### Opcionais

| Variável | Propósito | Tipo | Padrão | Exemplo |
|----------|-----------|------|--------|---------|
| `API_PORT` | Porta do servidor Fastify | Number | `4000` | `4000` |
| `LOG_LEVEL` | Nível de logging | String | `info` | `debug`, `info`, `warn`, `error` |
| `CORS_ORIGIN` | URLs permitidas para CORS | String | `http://localhost:3000` | `https://app.railway.app,https://docs.railway.app` |
| `AI_PROVIDER` | Provider de IA | String | `groq` | `groq`, `openai` (fallback) |
| `AI_MODEL` | Modelo de IA | String | `openai/gpt-oss-120b` | `openai/gpt-4` (mais caro) |
| `AI_TIMEOUT_MS` | Timeout de chamadas IA | Number | `5000` | `10000` (10 segundos) |

### Exemplo `.env` (Backend)

```bash
# Aplicação
NODE_ENV=development
API_PORT=4000
LOG_LEVEL=info

# Database
DATABASE_URL=postgresql://postgres:seazone_dev@localhost:5432/guia_hospede

# IA
GROQ_API_KEY=sk-proj-YOUR_API_KEY_HERE
AI_PROVIDER=groq
AI_MODEL=openai/gpt-oss-120b
AI_TIMEOUT_MS=5000

# CORS (local)
CORS_ORIGIN=http://localhost:3000
```

---

## Frontend

### Obrigatórias

| Variável | Propósito | Tipo | Exemplo Seguro |
|----------|-----------|------|-----------------|
| `NEXT_PUBLIC_API_URL` | URL do backend | String (URL) | `http://localhost:4000` |

### Opcionais

| Variável | Propósito | Tipo | Padrão | Exemplo |
|----------|-----------|------|--------|---------|
| `NEXT_PUBLIC_APP_NAME` | Nome da app (UI) | String | `Guia Digital` | `Seazone Guest Guide` |
| `NEXT_PUBLIC_APP_VERSION` | Versão exibida | String | `1.0.0` | `1.0.0` |

### Notas sobre `NEXT_PUBLIC_`

Variáveis com prefixo `NEXT_PUBLIC_` são **expostas ao navegador** (aparecem no HTML).

✅ **Seguro expor**:
- URLs do backend
- Nomes de aplicação
- Versão do app

❌ **Nunca expor**:
- Chaves de API
- Senhas
- Tokens de autenticação
- IDs internos

### Exemplo `.env.local` (Frontend)

```bash
# Backend
NEXT_PUBLIC_API_URL=http://localhost:4000

# Identificação (opcional)
NEXT_PUBLIC_APP_NAME=Guia Digital do Hóspede
NEXT_PUBLIC_APP_VERSION=1.0.0
```

---

## PostgreSQL (Docker)

Variáveis usadas em `docker-compose.yml`:

| Variável | Propósito | Tipo | Exemplo |
|----------|-----------|------|---------|
| `POSTGRES_USER` | Usuário root | String | `postgres` |
| `POSTGRES_PASSWORD` | Senha root | String | `seazone_dev` |
| `POSTGRES_DB` | Banco padrão | String | `guia_hospede` |

### Exemplo (docker-compose.yml)

```yaml
postgres:
  image: postgres:16-alpine
  environment:
    POSTGRES_USER: ${POSTGRES_USER}
    POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    POSTGRES_DB: ${POSTGRES_DB}
  volumes:
    - postgres_data:/var/lib/postgresql/data
  ports:
    - "5432:5432"
```

### Valores em `.env`

```bash
POSTGRES_USER=postgres
POSTGRES_PASSWORD=seazone_dev
POSTGRES_DB=guia_hospede
```

---

## Obtendo Chave Groq API

### Passo 1: Criar Conta

1. Acessar [https://console.groq.com](https://console.groq.com)
2. Clicar "Sign Up" (gratuito)
3. Preencher email, senha, aceitar termos

### Passo 2: Gerar Chave API

1. Dashboard → "API Keys" (ou "Keys")
2. Clicar "+ Create API Key"
3. Nomear (ex: "guia-hospede-dev")
4. Copiar chave gerada (`sk-proj-...`)

### Passo 3: Adicionar ao `.env`

```bash
GROQ_API_KEY=sk-proj-XxXxXxXx...
```

### Teste Rápido

```bash
# Verificar que a chave funciona
curl https://api.groq.com/test -H "Authorization: Bearer sk-proj-..."
```

---

## Variáveis por Ambiente

### Development (Local)

```bash
# .env
NODE_ENV=development
DATABASE_URL=postgresql://postgres:seazone_dev@localhost:5432/guia_hospede
GROQ_API_KEY=sk-proj-...
NEXT_PUBLIC_API_URL=http://localhost:4000
API_PORT=4000
LOG_LEVEL=debug
```

### Staging (Railway Test)

```bash
# Railway UI ou CLI
NODE_ENV=staging
DATABASE_URL=postgresql://user:SECURE_PASSWORD@db.railway.app:5432/guia_hospede
GROQ_API_KEY=sk-proj-... (mesmo da dev)
NEXT_PUBLIC_API_URL=https://api-staging.railway.app
API_PORT=4000 (Railway injeta automaticamente)
LOG_LEVEL=info
```

### Production (Railway Live)

```bash
# Railway UI ou CLI
NODE_ENV=production
DATABASE_URL=postgresql://user:VERY_SECURE_PASSWORD@db.railway.app:5432/guia_hospede
GROQ_API_KEY=sk-proj-... (preferir chave produção se tiver)
NEXT_PUBLIC_API_URL=https://api.railway.app
API_PORT=3000 (Railway)
LOG_LEVEL=warn
```

---

## Sincronizar com Backend

Backend usa **Prisma** que lê `DATABASE_URL` para:
1. Conectar ao banco
2. Fazer migrations
3. Validar schema

```bash
# Após alterar DATABASE_URL
cd backend
npx prisma migrate dev
npx prisma db push
```

---

## Railway Deployment

No Railway dashboard, você pode:

1. **Backend Service** → Settings → Variables:
   ```
   NODE_ENV = production
   GROQ_API_KEY = sk-proj-...
   DATABASE_URL = [gerado automaticamente]
   CORS_ORIGIN = https://frontend-service.railway.app
   ```

2. **Frontend Service** → Settings → Variables:
   ```
   NEXT_PUBLIC_API_URL = https://backend-service.railway.app
   NEXT_PUBLIC_APP_VERSION = 1.0.0
   ```

3. **Database** (Plugin PostgreSQL do Railway) → Automaticamente injeta `DATABASE_URL`

---

## Segurança: Boas Práticas

### ✅ Faça

- ✅ Use `.env.example` com placeholders seguros
- ✅ Adicione `.env*` ao `.gitignore`
- ✅ Rotacione chaves periodicamente (Groq, DB)
- ✅ Use variáveis de ambiente em Railway (UI ou CLI, não hard-code)
- ✅ Valide variáveis obrigatórias no startup

### ❌ Não Faça

- ❌ Commitar `.env` com chaves reais
- ❌ Compartilhar `.env` por email/Slack
- ❌ Usar chaves de prod em local
- ❌ Deixar variáveis hard-coded no código
- ❌ Logar valores de variáveis sensíveis

---

## Validação de Startup

Backend valida variáveis obrigatórias na inicialização:

```typescript
// backend/src/main.ts
if (!process.env.DATABASE_URL) {
  throw new Error('DATABASE_URL is required');
}

if (!process.env.GROQ_API_KEY) {
  throw new Error('GROQ_API_KEY is required');
}

console.log(`Starting Fastify on port ${process.env.API_PORT || 4000}`);
```

Se alguma variável obrigatória faltar, servidor não inicia (fail-fast).

---

**Próximo**: Veja como [Rodar Testes](/guia-de-uso/testes).
